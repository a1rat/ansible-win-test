---
- name: Display resource statistics (routeros)
  hosts: all
  gather_facts: false
  tasks:
    - name: test
      community.routeros.command:
        commands: /system resource print
      register: routeros_resources
    - debug:
        var: "routeros_resources.stdout_lines"
      changed_when: False
